{
    "name": "entry",
    "version": "1.0.0",
    "scripts": {
        "postinstall": "npm --prefix client/ install && npm run prepack && node setup.js --init",
        "update": "npm run prepack && node setup.js --update",
        "build": "tsc",
        "build:client": "npm run --prefix client/ build",
        "start": "node -r dotenv/config ./build/index.js",
        "dev": "npm run build:client && npm run dev:server",
        "dev:server": "ts-node -r dotenv/config ./src/index.ts",
        "prepack": "npm run build:client && npm run build",
        "pack": "pkg ."
    },
    "dependencies": {
        "aws-sdk": "^2.1199.0",
        "cors": "^2.8.5",
        "dotenv": "^16.0.1",
        "express": "^4.18.1",
        "helmet": "^5.1.1"
    },
    "devDependencies": {
        "@types/cors": "^2.8.12",
        "@types/express": "^4.17.13",
        "pkg": "^4.5.1",
        "pm2": "^5.2.0",
        "ts-node": "^10.9.1",
        "typescript": "^4.7.4"
    },
    "bin": {
        "runner": "build/index.js"
    },
    "pkg": {
        "assets": [
            "client/build/**/*"
        ],
        "targets": [
            "node10-macos",
            "node10-linux",
            "node10-win"
        ],
        "outputPath": "dist"
    }
}
